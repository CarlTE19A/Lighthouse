#ifndef _SERIALINPUT_H    // Put these two lines at the top of your file.
#define _SERIALINPUT_H    // (Use a suitable name, usually based on the file name.)//Define Pins for rotation encoder
#include "Arduino.h"

int SerialInput();
#endif
